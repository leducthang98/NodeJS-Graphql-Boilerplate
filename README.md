# setup and build project

## development
recommend: use node version latest

initial setup:
```
$ cp .env.example .env
$ yarn install
$ docker-compose up -d
$ yarn deploy
```

server startup:
```
$ yarn start
```

